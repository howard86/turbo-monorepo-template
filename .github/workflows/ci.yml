name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    uses: howard86/actions/.github/workflows/ci.yml@402ce305af6a7cd037b84c13b41134f451708cdc # v2.0.2
    permissions:
      contents: read
    with:
      run-test: true
      run-build: true
